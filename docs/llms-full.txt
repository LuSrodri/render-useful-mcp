# render-useful-mcp — full reference

> Unofficial MCP server for the Render Public API. 213 tools: all 208 endpoints of Render's Public API (spec 1.0.0), generated from Render's OpenAPI document, plus 5 workflow tools. Local stdio server, MIT licensed, no telemetry. Not affiliated with Render.

Source: https://github.com/LuSrodri/render-useful-mcp
Package: https://www.npmjs.com/package/render-useful-mcp
Docs: https://lusrodri.github.io/render-useful-mcp/

## Install

### Claude Code (global — recommended)

```
claude mcp add render --scope user -e RENDER_API_KEY=rnd_your_key -- npx -y render-useful-mcp
```

`--scope user` registers the server in your user configuration, so it is available in every
project. The default scope is `local`, which registers it for the current directory only —
the usual reason a server "disappears" after changing directory.

Verify with `claude mcp list`.

### Claude Code plugin

```
/plugin marketplace add LuSrodri/render-useful-mcp
/plugin install render-useful-mcp@lusrodri-render
```

Export `RENDER_API_KEY` in the shell that launches Claude Code.

### Claude Desktop and other mcpServers clients

```json
{
  "mcpServers": {
    "render": {
      "command": "npx",
      "args": ["-y", "render-useful-mcp"],
      "env": {
        "RENDER_API_KEY": "rnd_your_key_here",
        "RENDER_WORKSPACE_ID": "tea_your_workspace_id"
      }
    }
  }
}
```

### Desktop extension

Download the `.mcpb` bundle from https://github.com/LuSrodri/render-useful-mcp/releases/latest and open it. Claude installs it
and prompts for the API key; the bundle ships its own dependencies and needs no Node install.

## Configuration

| Variable | Default | Description |
| --- | --- | --- |
| `RENDER_API_KEY` | — | Required. Render API key. |
| `RENDER_WORKSPACE_ID` | — | Workspace id used wherever an `ownerId` is needed and none was given. |
| `RENDER_MCP_TOOLSETS` | `all` | Narrow the surface to a comma-separated list of toolsets. |
| `RENDER_MCP_READ_ONLY` | `false` | Expose only non-mutating tools. |
| `RENDER_MCP_DYNAMIC_TOOLSETS` | `true` | Register `render_toolsets`. |
| `RENDER_MCP_TIMEOUT_MS` | `60000` | Per-request timeout. |
| `RENDER_MCP_MAX_RETRIES` | `3` | Retries for rate limits and transient server errors. |
| `RENDER_MCP_MAX_RESPONSE_BYTES` | `400000` | Larger tool results are truncated with a note. |
| `RENDER_MCP_LOG_LEVEL` | `info` | `debug`, `info`, `warn`, `error`, `silent`. Logs go to stderr. |
| `RENDER_API_BASE_URL` | `https://api.render.com/v1` | Override for proxies or testing. |

## Common tasks

**Create a cron job that runs a Docker image on a schedule.** Cron jobs are services. Call
`render_create_service` with `type: "cron_job"`, and a `serviceDetails` matching the
`cronJobDetailsPOST` branch:

```json
{
  "type": "cron_job",
  "name": "nightly-report",
  "ownerId": "tea-...",
  "repo": "https://github.com/acme/reports",
  "branch": "main",
  "serviceDetails": {
    "runtime": "docker",
    "schedule": "0 3 * * *",
    "plan": "starter",
    "region": "oregon",
    "envSpecificDetails": {
      "dockerfilePath": "./Dockerfile",
      "dockerContext": ".",
      "dockerCommand": "python report.py"
    }
  }
}
```

For a prebuilt image, drop `repo`/`branch`, set `image` to
`{"ownerId":"tea-...","imagePath":"docker.io/acme/reports:latest"}`, use `runtime: "image"`,
and give `envSpecificDetails` only `dockerCommand`. `schedule` is a five-field cron
expression in UTC. Change it later with `render_update_service`; trigger an off-schedule run
with `render_run_cron_job`. `render_create_job` is a different thing — a one-off command on
an existing service.

**Diagnose a broken service.** `render_service_status` returns configuration, recent
deploys, instances and error logs in one call.

**Deploy and wait.** `render_create_deploy`, then `render_wait_for_deploy`.

**Read logs.** `render_recent_logs` takes a service name; `render_list_logs` needs an
`ownerId` and a resource array.

## Conventions

- Ids are prefixed: services `srv-`, deploys `dep-`, workspaces `tea-`/`usr-`, Postgres
  `dpg-`, Key Value `red-`, environment groups `evg-`.
- Users say names, tools need ids — `render_find_service` resolves partial and approximate
  names in one call.
- Where an argument is a `oneOf`, each branch is titled with its name in Render's spec.
  Match the title to the field that selects it: `serviceDetails` follows `type`,
  `envSpecificDetails` follows `runtime` (`docker` → `dockerDetails`).
- PUT tools that take a collection replace it wholesale; read the current value first.

## Workflow tools

Always available, in any toolset configuration.

- `render_find_service` — Resolves a service name — including a partial or approximate one — to a single Render service, returning its id plus close alternatives.
- `render_recent_logs` — Fetches recent log lines for a service, resolving the service name and workspace id for you.
- `render_service_status` — One-call triage for a service: its configuration, latest deploys, running instances and most recent error-level logs.
- `render_toolsets` — Lists every Render toolset with its tool count and whether it is currently enabled.
- `render_wait_for_deploy` — Polls a deploy until it reaches a terminal state (live, build_failed, update_failed, canceled, deactivated) or the timeout expires.

## API tools by toolset

One tool per Render endpoint, named `render_<operation_id>`. All toolsets are on by default;
`RENDER_MCP_TOOLSETS` narrows the surface.

### services (43 tools)

Services, deploys, custom domains, one-off jobs, cron job runs and events.

- `render_add_or_update_secret_file` — Add or update secret file.
- `render_autoscale_service` — Update autoscaling config.
- `render_cancel_cron_job_run` — Cancel running cron job.
- `render_cancel_deploy` — Cancel deploy.
- `render_cancel_job` — Cancel running job.
- `render_create_custom_domain` — Add custom domain.
- `render_create_deploy` — Trigger deploy.
- `render_create_job` — Create job.
- `render_create_service` — Create service.
- `render_delete_autoscaling_config` — Delete autoscaling config.
- `render_delete_custom_domain` — Delete custom domain.
- `render_delete_env_var` — Delete environment variable.
- `render_delete_secret_file` — Delete secret file.
- `render_delete_service` — Delete service.
- `render_get_env_vars_for_service` — List environment variables.
- `render_list_custom_domains` — List custom domains.
- `render_list_deploys` — List deploys.
- `render_list_events` — List events.
- `render_list_instances` — List instances.
- `render_list_jobs` — List jobs.
- `render_list_secret_files_for_service` — List secret files.
- `render_list_services` — List services.
- `render_preview_service` — Create service preview (image-backed).
- `render_purge_cache` — Purge Web Service Cache.
- `render_refresh_custom_domain` — Verify DNS configuration.
- `render_restart_service` — Restart service.
- `render_resume_service` — Resume service.
- `render_retrieve_custom_domain` — Retrieve custom domain.
- `render_retrieve_deploy` — Retrieve deploy.
- `render_retrieve_env_var` — Retrieve environment variable.
- `render_retrieve_event` — Retrieve event.
- `render_retrieve_job` — Retrieve job.
- `render_retrieve_secret_file` — Retrieve secret file.
- `render_retrieve_service` — Retrieve service.
- `render_retrieve_service_outbound_ips` — Retrieve service outbound IPs.
- `render_rollback_deploy` — Roll back deploy.
- `render_run_cron_job` — Trigger cron job run.
- `render_scale_service` — Scale instance count.
- `render_suspend_service` — Suspend service.
- `render_update_env_var` — Add or update environment variable.
- `render_update_env_vars_for_service` — Update environment variables.
- `render_update_secret_files_for_service` — Update secret files.
- `render_update_service` — Update service.

### metrics (23 tools)

CPU, memory, bandwidth, HTTP, disk and connection metrics, plus metrics streams.

- `render_delete_owner_metrics_stream` — Delete metrics stream.
- `render_get_active_connections` — Get active connection count.
- `render_get_bandwidth` — Get bandwidth usage.
- `render_get_bandwidth_sources` — Get bandwidth usage breakdown by traffic source.
- `render_get_cpu` — Get CPU usage.
- `render_get_cpu_limit` — Get CPU limit.
- `render_get_cpu_target` — Get CPU target.
- `render_get_disk_capacity` — Get disk capacity.
- `render_get_disk_usage` — Get disk usage.
- `render_get_http_latency` — Get HTTP latency.
- `render_get_http_requests` — Get HTTP request count.
- `render_get_instance_count` — Get instance count.
- `render_get_memory` — Get memory usage.
- `render_get_memory_limit` — Get memory limit.
- `render_get_memory_target` — Get memory target.
- `render_get_owner_metrics_stream` — Retrieve metrics stream.
- `render_get_replication_lag` — Get replica lag.
- `render_get_task_runs_completed` — Get task runs completed count.
- `render_get_task_runs_queued` — Get task runs queued count.
- `render_list_application_filter_values` — List queryable instance values.
- `render_list_http_filter_values` — List queryable status codes and host values.
- `render_list_path_filter_values` — List queryable paths.
- `render_upsert_owner_metrics_stream` — Create or update metrics stream.

### postgres (21 tools)

Postgres instances, users, exports, recovery and query insights.

- `render_create_postgres` — Create Postgres instance.
- `render_create_postgres_export` — Create Postgres export.
- `render_create_postgres_user` — Create PostgreSQL User.
- `render_delete_postgres` — Delete Postgres instance.
- `render_delete_postgres_user` — Delete PostgreSQL User.
- `render_failover_postgres` — Failover Postgres instance.
- `render_list_postgres` — List Postgres instances.
- `render_list_postgres_export` — List Postgres exports.
- `render_list_postgres_processes` — List live queries.
- `render_list_postgres_sizes` — List database, table, and index sizes.
- `render_list_postgres_table_scans` — List table scans.
- `render_list_postgres_top_queries` — List top queries.
- `render_list_postgres_users` — List PostgreSQL Users.
- `render_recover_postgres` — Trigger point-in-time recovery.
- `render_restart_postgres` — Restart Postgres instance.
- `render_resume_postgres` — Resume Postgres instance.
- `render_retrieve_postgres` — Retrieve Postgres instance.
- `render_retrieve_postgres_connection_info` — Retrieve Postgres connection info.
- `render_retrieve_postgres_recovery_info` — Retrieve point-in-time recovery status.
- `render_suspend_postgres` — Suspend Postgres instance.
- `render_update_postgres` — Update Postgres instance.

### workflows (15 tools)

Render Workflows and workflow tasks (public beta).

- `render_cancel_task_run` — Cancel task run.
- `render_create_task` — Run task.
- `render_create_workflow` — Create a workflow.
- `render_create_workflow_version` — Deploy a workflow version.
- `render_delete_workflow` — Delete workflow.
- `render_get_task` — Retrieve task.
- `render_get_task_run` — Retrieve task run.
- `render_get_workflow` — Retrieve workflow.
- `render_get_workflow_version` — Retrieve workflow version.
- `render_list_task_runs` — List task runs.
- `render_list_tasks` — List tasks.
- `render_list_workflow_versions` — List workflow versions.
- `render_list_workflows` — List workflows.
- `render_stream_task_runs_events` — Stream realtime events (SSE).
- `render_update_workflow` — Update workflow.

### env-groups (13 tools)

Environment groups, their variables and secret files.

- `render_create_env_group` — Create environment group.
- `render_delete_env_group` — Delete environment group.
- `render_delete_env_group_env_var` — Remove environment variable.
- `render_delete_env_group_secret_file` — Remove secret file.
- `render_link_service_to_env_group` — Link service.
- `render_list_env_groups` — List environment groups.
- `render_retrieve_env_group` — Retrieve environment group.
- `render_retrieve_env_group_env_var` — Retrieve environment variable.
- `render_retrieve_env_group_secret_file` — Retrieve secret file.
- `render_unlink_service_from_env_group` — Unlink service.
- `render_update_env_group` — Update environment group.
- `render_update_env_group_env_var` — Add or update environment variable.
- `render_update_env_group_secret_file` — Add or update secret file.

### projects (12 tools)

Projects and environments.

- `render_add_resources_to_environment` — Add resources to environment.
- `render_create_environment` — Create environment.
- `render_create_project` — Create project.
- `render_delete_environment` — Delete environment.
- `render_delete_project` — Delete project.
- `render_list_environments` — List environments.
- `render_list_projects` — List projects.
- `render_remove_resources_from_environment` — Remove resources from environment.
- `render_retrieve_environment` — Retrieve environment.
- `render_retrieve_project` — Retrieve Project.
- `render_update_environment` — Update environment.
- `render_update_project` — Update project.

### webhooks (11 tools)

Webhooks and notification settings/overrides.

- `render_create_webhook` — Create a webhook.
- `render_delete_webhook` — Delete a webhook.
- `render_list_notification_overrides` — List notification overrides.
- `render_list_webhook_events` — List webhook events.
- `render_list_webhooks` — List webhooks.
- `render_patch_owner_notification_settings` — Update notification settings.
- `render_patch_service_notification_overrides` — Update notification override.
- `render_retrieve_owner_notification_settings` — Retrieve notification settings.
- `render_retrieve_service_notification_overrides` — Retrieve notification override.
- `render_retrieve_webhook` — Retrieve a webhook.
- `render_update_webhook` — Update a webhook.

### logs (10 tools)

Log queries, label discovery and log stream configuration.

- `render_delete_owner_log_stream` — Delete log stream.
- `render_delete_resource_log_stream` — Delete log stream override.
- `render_get_owner_log_stream` — Retrieve log stream.
- `render_get_resource_log_stream` — Retrieve log stream override.
- `render_list_logs` — List logs.
- `render_list_logs_values` — List log label values.
- `render_list_resource_log_streams` — List log stream overrides.
- `render_subscribe_logs` — Subscribe to new logs.
- `render_update_owner_log_stream` — Update log stream.
- `render_update_resource_log_stream` — Update log stream override.

### static-sites (9 tools)

Header rules and redirect/rewrite routes for static sites.

- `render_add_headers` — Add header rule.
- `render_add_route` — Add redirect/rewrite rules.
- `render_delete_header` — Delete header rule.
- `render_delete_route` — Delete redirect/rewrite rule.
- `render_list_headers` — List header rules.
- `render_list_routes` — List redirect/rewrite rules.
- `render_patch_route` — Update redirect/rewrite rule priority.
- `render_put_routes` — Update redirect/rewrite rules.
- `render_update_headers` — Replace header rules.

### deprecated (8 tools)

Legacy Redis endpoints that Render has superseded by the Key Value API.

- `render_create_redis` — [DEPRECATED by Render] Create Redis instance.
- `render_delete_redis` — [DEPRECATED by Render] Delete Redis instance.
- `render_list_redis` — [DEPRECATED by Render] List Redis instances.
- `render_resume_redis` — [DEPRECATED by Render] Resume Redis instance.
- `render_retrieve_redis` — [DEPRECATED by Render] Retrieve Redis instance.
- `render_retrieve_redis_connection_info` — [DEPRECATED by Render] Retrieve Redis connection info.
- `render_suspend_redis` — [DEPRECATED by Render] Suspend Redis instance.
- `render_update_redis` — [DEPRECATED by Render] Update Redis instance.

### key-value (8 tools)

Key Value (Redis-compatible) instances and connection info.

- `render_create_key_value` — Create Key Value instance.
- `render_delete_key_value` — Delete Key Value instance.
- `render_list_key_value` — List Key Value instances.
- `render_resume_key_value` — Resume Key Value instance.
- `render_retrieve_key_value` — Retrieve Key Value instance.
- `render_retrieve_key_value_connection_info` — Retrieve Key Value connection info.
- `render_suspend_key_value` — Suspend Key Value instance.
- `render_update_key_value` — Update Key Value instance.

### workspaces (8 tools)

Workspaces, members, the authenticated user and audit logs.

- `render_get_user` — Get the authenticated user.
- `render_list_organization_audit_logs` — List organization audit logs.
- `render_list_owner_audit_logs` — List workspace audit logs.
- `render_list_owners` — List workspaces.
- `render_remove_workspace_member` — Remove workspace member.
- `render_retrieve_owner` — Retrieve workspace.
- `render_retrieve_owner_members` — List workspace members.
- `render_update_workspace_member` — Update workspace member role.

### disks (7 tools)

Persistent disks and their snapshots.

- `render_add_disk` — Add disk.
- `render_delete_disk` — Delete disk.
- `render_list_disks` — List disks.
- `render_list_snapshots` — List snapshots.
- `render_restore_snapshot` — Restore snapshot.
- `render_retrieve_disk` — Retrieve disk.
- `render_update_disk` — Update disk.

### blueprints (6 tools)

Blueprints and Blueprint syncs.

- `render_disconnect_blueprint` — Disconnect Blueprint.
- `render_list_blueprint_syncs` — List Blueprint syncs.
- `render_list_blueprints` — List Blueprints.
- `render_retrieve_blueprint` — Retrieve Blueprint.
- `render_update_blueprint` — Update Blueprint.
- `render_validate_blueprint` — Validate Blueprint.

### network (5 tools)

Dedicated outbound IP sets.

- `render_create_dedicated_ip` — Create dedicated IP set.
- `render_delete_dedicated_ip` — Delete dedicated IP set.
- `render_list_dedicated_ips` — List dedicated IPs.
- `render_retrieve_dedicated_ip` — Retrieve dedicated IP set.
- `render_update_dedicated_ip` — Update dedicated IP set.

### registry (5 tools)

Container registry credentials.

- `render_create_registry_credential` — Create registry credential.
- `render_delete_registry_credential` — Delete registry credential.
- `render_list_registry_credentials` — List registry credentials.
- `render_retrieve_registry_credential` — Retrieve registry credential.
- `render_update_registry_credential` — Update registry credential.

### maintenance (4 tools)

Scheduled maintenance runs.

- `render_list_maintenance` — List maintenance runs.
- `render_retrieve_maintenance` — Retrieve maintenance run.
- `render_trigger_maintenance` — Trigger maintenance run.
- `render_update_maintenance` — Update maintenance run.
